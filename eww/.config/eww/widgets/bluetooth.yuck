(defwidget bluetooth-widget []
  (button
    :class "module ${bluetooth.class}"
    :onclick "blueman-manager &"
    :onrightclick "scripts/bluetooth connect"
    :tooltip "${bluetooth.text} ${bluetooth.battery}"
  {bluetooth.icon}))
